services:
  market-data-test:
    build:
      context: .
      args:
        GITHUB_ACTOR:
        GITHUB_TOKEN:
    container_name: gnomepy-testing
    environment:
      - LISTING_ID=8
      - DURATION=10
      - OUTPUT_DIR=/app/output
      - PROXY_PORT=8765
      - STAGE=dev
      - GNOME_REGISTRY_API_KEY=${GNOME_REGISTRY_API_KEY}
    volumes:
      - ./output:/app/output
